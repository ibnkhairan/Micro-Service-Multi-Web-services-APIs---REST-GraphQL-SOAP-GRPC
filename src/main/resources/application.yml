spring:
  application:
    name: customer-service
  datasource:
    url: jdbc:h2:mem:customer-db
  h2:
    console:
      enabled: true
  graphql:
    graphiql:
      enabled: true
server:
  port: 8082